#include "main.h"

/**
 * mul - multiplies two numbers
 *
 * @a: is the first number
 * @b: is the second number
 * Return: the product on success otherwise 0
 */
int mul(int a, int b)
{
	return (a * b);
}
