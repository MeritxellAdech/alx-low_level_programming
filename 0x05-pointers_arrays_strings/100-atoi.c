#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - convert a string to an integer
 * @s: is the string to be converted to integer
 * Return: success 0
 */
int _atoi(char *s)
{
	return (atoi(s));
}
